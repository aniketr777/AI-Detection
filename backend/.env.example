# Backend Server Environment Variables
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
# Set to the authorized owner's public IP address.
# The dashboard will show a security alert when the site is visited from any other IP.
# Leave unset to only treat localhost as authorized.
ALLOWED_VISITOR_IP=
